def findLetter(string, letter):
    
    for index in string:
        if string[index] == letter:
            print "Found it!"
            
def findIndex(string, letter):

    for index in string:
        if string[index] == letter:
            print index
            
def findNumber(list, number) # exactly the same as first one

    for index in list:
        if list[index] == number
            print "found it!"
    