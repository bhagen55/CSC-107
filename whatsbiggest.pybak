# take a list
# store first value of list
# check next value
  # if next value is bigger than first, set stored number to second number
  # if next value is smaller than first, don't change stored number
# print stored value at end of list

def whatsBiggest(list):
    biggest = list[0]
    for compare in list:
        if number > biggest:
            biggest = number
    print(biggest)